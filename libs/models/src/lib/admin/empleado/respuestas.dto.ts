import {DeptoDto, EmpleadoDto} from "@sistema-comercial/models";
import {PickType} from "@nestjs/graphql";

// export class StateAuth extends PickType(EmpleadoDto, ["_id", "avatar", "nombreCompleto", "deptoId", "auth"] as const)
// {
//     deptoEmpleado: DeptoDto
// }
